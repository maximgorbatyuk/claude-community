---
import Layout from '../../layouts/Layout.astro';
import PlaceholderPage from '../../components/PlaceholderPage.astro';
import { t } from '../../i18n';
---

<Layout title={t('en', 'about.meta.title')} description={t('en', 'meta.description')} lang="en">
	<PlaceholderPage lang="en" pageTitle={t('en', 'nav.about')} />
</Layout>
